# Production Build Environment (temporary for build time)
# These will be overridden by actual server environment variables
DATABASE_URL="mysql://root:@localhost:3306/web_bds"
JWT_SECRET="temporary-build-secret"
NODE_ENV="production"
